import React from "react";
import "./Header.css";

const Header = props => (
  <header className="header">

    <h2>
      Click on an image only once to get points.
    </h2>
  </header>
);

export default Header;
